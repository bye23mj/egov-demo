# Confluence 동기화
requests>=2.31.0
html2text>=2020.1.16
markdown>=3.5.0

# JIRA 연동
# requests는 이미 위에서 포함됨

# 문서 변환 (Phase 2 - 문서 정규화)
python-docx>=0.8.11          # DOCX 읽기/쓰기
openpyxl>=3.1.0              # XLSX 읽기
python-pptx>=0.6.21          # PPTX 읽기
PyPDF2>=3.0.0                # PDF 텍스트 추출

# 한글 문서 (HWPX) - 선택사항
# python-hwp>=0.1.0           # HWPX 읽기 (별도 설치 필요)

# 유틸리티
python-dateutil>=2.8.2
