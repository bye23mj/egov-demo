confluence-sync 스킬을 실행해줘. 다음 명령어로 Confluence 문서를 동기화한다:

```bash
cd /Users/ai/vscode/egov-demo && python scripts/confluence-sync.py sync
```

- download: Confluence → 로컬 다운로드
- upload: 로컬 신규 문서 → Confluence 업로드  
- sync: 양방향 동기화

$ARGUMENTS
